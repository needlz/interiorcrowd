- content_for :head do
  = javascript_include_tag 'shared/color_selector'
  = javascript_include_tag 'contests/show'

#in_pricing
  .container
    .section_header
      \ 
    .row.charts_wrapp
      .col-sm-12
        .plan
          .wrapper
            .row
              .page-header
                %h2 Design Center
            .row
              .page-header
                %h3
                  Previewing  
                  %strong.price
                    = @contest_view.name
                    = Contest::CONTEST_DESIGN_BUDGET_PLAN[@contest_view.budget_plan.to_i]
            .col-sm-offset-1.preview
              = render partial: 'contests/preview'
            .row.formbuttons
              .col-sm-5.col-sm-offset-1
                %a.order{:href => contests_path, :style => "width:70%"} Go back to other contests
              - if @request.blank?
                .col-sm-4.col-sm-offset-2
                  %a.order{:href => respond_contest_path(params[:id]), :style => "width:55%"} Join this contest
