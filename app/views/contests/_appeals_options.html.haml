.row
  .col-sm-7.col-sm-offset-1
    .plan.appeals
      %table.table.table-hover
        = render partial: 'contests/appeal_scale_row',
            collection: @contest_view.appeal_scales,
            as: :scale