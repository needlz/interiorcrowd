.col-md-10.col-md-offset-1.text-center.design-packages
  %h3.font36= t('contests.creation.packages.title')
  %p= t('contests.creation.packages.description')
  .smallUnderline
  .row
    - ContestCreationWizard.budget_plans.each_with_index do |package, index|
      .col-sm-4{ class: ('middle-package' if (index + 1).modulo(2).zero?) }
        .head-design-package
          %h5= package.name
        - package.services.each do |service|
          %p= service.html_safe
