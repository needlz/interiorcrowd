#customer.tab-pane.active{:role => "tabpanel"}
  #accordion.panel-group{"aria-multiselectable" => "false", :role => "tablist"}
    - faq_client_questions.each_with_index do |question, index|
      = render partial: 'home/faq/spoiler_item', locals: { number: index + 1,
          faq_item: faq_item('customer', question) }
