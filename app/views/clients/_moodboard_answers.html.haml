.clearfix
.voteBox.answers{ data: { url: answer_contest_request_path(id: request_id) } }
  - ContestRequest::ANSWERS.each do |answer_option|
    = render partial: 'clients/answer',
        locals: { answer: answer,
                  text: t("client_center.entries.answers.#{ answer_option }"),
                  css_class: (button_class if defined?(button_class)) }
  .clearfix
