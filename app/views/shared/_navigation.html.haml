%ul.nav.nav-tabs.text-menu.pull-right{:style => "border:0"}
  - @navigation.tabs.each do |tab, navigation_link|
    = render partial: 'shared/navigation_link', locals: navigation_link.merge(tab: tab)
