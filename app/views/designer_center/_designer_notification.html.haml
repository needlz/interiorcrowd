.notification.row{type: notification.type}
  .leftBorderNotification{:class => "#{ notification.color }Label"}
  %a{ href: notification.path(current_user) }
    %p.designerNotification{ class: notification.read_class }
      = notification.text
